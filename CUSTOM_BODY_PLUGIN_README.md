# 自定义身体绑定 — Maya 脚本插件 v1.2.0

原生 Maya 窗口、骨骼、控制器和约束，无需 MCP、AI 模型、网络、pip 或第三方绑定插件。
已在 Windows / Maya 2024.2 验证。场景单位为厘米和角度 deg。

## 安装与打开

1. 把独立 ZIP 解压到准备长期保留的位置。
2. 将 `install_custom_body_rig.mel` 拖进 Maya 视口；也支持拖入 `install_custom_body_rig.py`。
3. 自动打开「自定义身体绑定」窗口，并在 `CustomBodyRig` 工具架添加「身体绑定」按钮。
4. 后续点击工具架按钮打开。目录移动后重新拖入安装文件即可更新按钮路径。

安装程序只添加自己的工具架按钮，不需要重开场景，不修改其他插件的启动脚本。
ZIP 里的 `maya_agent/rigs/custom_body/` 是独立绑定模块；上层 `__init__.py` 仅提供包入口。

## 标准模式

不勾选「在选择物体上创建并驱动」，设置名称前缀、骨骼/控制器数量（2–64）及总高度。
默认四节保留之前确认的绑定行为。重复创建自动分配新命名空间。

## 在选择物体上创建并驱动

1. 先打开工具窗口，窗口会开启 Maya 的「追踪选择顺序」。
2. 从腰部到胸部逐个选择骨骼或物体，避免框选产生不明确的顺序。
3. 勾选「在选择物体上创建并驱动」，或点击「读取当前选择」。
4. 检查列表：第一项为腰部，最后一项为胸部。可用上移、下移调整顺序。
5. 点击创建。使用当前显示的列表顺序，数量自动取列表长度；手动数量和高度字段在此模式禁用。

每个目标对应一个新骨骼、一个控制器和一个输出父约束。目标位置与朝向匹配，控制器初始平移/旋转为零。
创建时保持目标原层级、姿态、蒙皮；约束保持偏移，之后移动或旋转新控制器即可驱动对应目标。
中段权重按目标间的累计距离计算，支持弯曲、非等距排列和不同朝向。不会自动重新蒙皮。

“总高度”在选择模式返回沿列表的累计距离，不是竖直高度。

## 目标检查与撤销

- 接受 2–64 个不重复的 transform / joint，不接受顶点组件、引用节点或实例目标。
- 目标平移、旋转须可写；目标及父级不得已有平移/旋转/offsetParentMatrix 动画或驱动连接。
- 不自动解锁、删除已有约束或覆盖动画。出现冲突会在窗口底部显示具体节点或通道。
- 暂不支持目标世界变换中的非均匀缩放、负缩放或剪切；相邻目标不能重合。
- 整次创建可 Ctrl+Z 撤销，Ctrl+Y 重做；不要在创建后改变驱动层级或删除内部矩阵节点。
- 构建失败会清理本次新节点并恢复目标通道。
- 选定目标约束驱动的是平移和旋转，不额外连接目标缩放。

## Python 调用

```python
from maya_agent.rigs.custom_body import build, show_ui
show_ui()
rig = build(segment_count=8, height=12)
rig = build(namespace='heroSpine', use_selection=True)
rig = build(namespace='heroSpine', targets=['|root|waist', '|root|chest'])
```

窗口和 MCP 共享同一 `build()` 实现；后续扩展修改 `ui.py`、`targets.py`、`fitting.py` 等组件。
原始标准模式的局部矩阵计算仍有整体缩放/任意父级变换限制；本版不提供整体缩放控制器。

MIT License，保留 Maya Agent Contributors 版权声明。详见随附 LICENSE。
